<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
</head>

<body>
    <h2>Edit Product</h2>
    <form action="/products/<%=product._id%>?_method=PUT" method="POST">
        <label for="product">Product Name:</label>
        <input type="text" name="name" id="product" placeholder="product name" value="<%=product.name%>">
        <label for="price">Price(Rs/Unit):</label>
        <input type="number" id="price" name="price" placeholder="price" value="<%=product.price%>">
        <label for="category">Category:</label>
        <select name="category" id="category">
            <% for(let category of categories) { %>
                <option value="<%=category%>" <%=product.category===category ? 'selected' : '' %> >
                    <%=category %>
                </option>
                <% } %>
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%=product._id%> ">Product Details Page</a>
    <a href="/products">All Products</a>
    <a href="/products/<%=product._id%> ">Cancel</a>

</body>

</html>